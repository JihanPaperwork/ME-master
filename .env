# JWT Secret for authentication (still required)
JWT_SECRET=YOUR_VERY_STRONG_JWT_SECRET_HERE_LIKE_RANDOM_STRING_123ABC

# Optional: You can keep the PORT variable if you want to explicitly define the backend port
PORT=3000